import { EnumLabelValueLisPipe } from './enum-label-value-list.pipe';

describe('EnumLabelValueLisPipe', () => {
  it('create an instance', () => {
    const pipe = new EnumLabelValueLisPipe();
    expect(pipe).toBeTruthy();
  });
});
